plugins {
    id "net.fabricmc.fabric-loom" version "1.15-SNAPSHOT"
}

version = "1.0.0"
group = "com.example"

dependencies {
    implementation "net.fabricmc:fabric-loader:0.18.4"
    implementation "net.fabricmc.fabric-api:fabric-api:0.100.1+26.1"
}

tasks.withType(Jar) {
    archiveClassifier.set("dev")
}
